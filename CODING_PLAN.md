# Coding Plan - Token 优化方案
## 目标：在20元/天预算内，高效完成所有任务

---

## 一、核心原则

### 1. 成本分层
| 层级 | 成本 | 使用策略 |
|-----|------|---------|
| 🟢 低成本 | < ¥0.5 | 自由使用，无限制 |
| 🟡 中成本 | ¥0.5-1.0 | 批量处理，减少次数 |
| 🟠 高成本 | ¥1.0-2.0 | 必需时才用，优化参数 |
| 🔴 极高成本 | > ¥2.0 | 严格审批，寻找替代方案 |

### 2. 批量处理原则
- ❌ 分散调用：`message.send()` 5次 = 5次API调用
- ✅ 批量调用：合并为1次，节省80%开销

### 3. 缓存复用原则
- 图片生成一次，多处使用
- 数据下载一次，本地缓存
- 分析结果保存，避免重复计算

---

## 二、具体优化策略

### 策略1：消息合并（节省60-80% API调用）

**Before（差）：**
```python
message.send("第一部分")
message.send("第二部分")  # 第2次调用
message.send("第三部分")  # 第3次调用
```

**After（优）：**
```python
# 合并为一次
content = """第一部分

第二部分

第三部分"""
message.send(content)  # 只调用1次
```

### 策略2：避免失败重试（节省50% Token）

**问题操作（高失败率）：**
- `web_search` - 未配置API Key，每次调用都失败
- `browser` - 插件未连接，反复尝试
- `akshare` 超时 - 网络问题反复重试

**解决方案：**
```python
# 检查前置条件，不满足直接跳过
def safe_web_search(query):
    if not has_brave_api_key():
        logger.info("跳过搜索：API未配置")
        return None  # 直接返回，不消耗Token
    
    # 配置好才执行
    return web_search(query)
```

### 策略3：精简上下文（节省30-40% Token）

**问题：**
- 会话越长，每次回复携带的历史越多
- 132k上下文 = 每次回复都携带132k

**解决方案：**
```python
# 每2小时或每50轮对话，新建会话
def should_new_session():
    if context_size > 100000:  # 100k阈值
        return True
    if message_count > 50:
        return True
    return False

# 新会话时加载必要记忆
if new_session:
    load_from_memory()  # 只加载关键信息
```

### 策略4：图片优化（节省20-30%）

**Before：**
- 反复生成图片
- 高清大图上传飞书（失败率高）

**After：**
- 一次生成，保存桌面
- 飞书只发文字+本地路径
- 用户需要时自己查看

### 策略5：工具调用管控（节省40-50%）

**高危操作清单：**
| 工具 | 预估成本 | 优化方案 |
|-----|---------|---------|
| web_search | ¥2.0/次 | 未配置时禁用 |
| browser | ¥3.0/次 | 未连接时禁用 |
| exec长任务 | ¥1.0/次 | 设置超时，避免死循环 |
| canvas | ¥1.5/次 | 未配置时禁用 |

---

## 三、明日执行计划（3月2日）

### 08:00 盘前推送
**预算：** ¥1.0以内
**执行：**
```python
# 1. 获取数据（轻量）- ¥0.2
akshare.get_daily()  # 只取必要数据

# 2. 模型推理（轻量）- ¥0.3
model.predict()  # 已有模型，只推理

# 3. 生成摘要（轻量）- ¥0.3
generate_summary()  # 纯文字，无复杂分析

# 4. 合并发送（1次API）- ¥0.2
send_combined_message(text)  # 不附加图片
```

### 09:30 盘中跟踪
**预算：** ¥0.5以内
**执行：**
- 只监控，不主动推送
- 用户询问时才回复

### 15:35 盘后总结
**预算：** ¥1.5以内
**执行：**
- 数据分析（中等复杂度）
- 生成简单报告
- 合并发送

**全天预算：** ¥3.0以内（远低于20元上限）

---

## 四、Coding Checklist

每次编码前检查：
- [ ] 这个操作必要吗？能否用缓存？
- [ ] 预估成本多少？在预算内吗？
- [ ] 能否批量处理，减少调用次数？
- [ ] 前置条件满足吗？避免失败重试
- [ ] 上下文是否过长？需要新建会话吗？

---

## 五、应急方案

如果意外超支：

**¥15元时（警告）：**
- 停止非核心任务
- 只保留：数据获取、简单回复
- 禁用：复杂分析、图片生成

**¥18元时（严控）：**
- 仅保留：用户主动询问
- 停止所有自动任务
- 明日再继续

**¥20元时（停止）：**
- 完全停止，等待明日重置
- 向用户报告："今日预算已用完"

---

## 六、监控机制

每小时自检：
```python
def hourly_check():
    status = budget.get_daily_report()
    
    if status['usage_rate'] > 75%:
        logger.warning(f"Token使用75%: ¥{status['spent']}")
    
    if status['remaining'] < 5:
        logger.warning("剩余预算不足5元")
```

---

## 总结

**目标：** ¥20/天预算，高效完成任务
**核心策略：**
1. 批量合并，减少API调用
2. 前置检查，避免失败重试
3. 定期清理，控制上下文
4. 缓存复用，减少重复计算

**预期效果：**
- 每日实际消耗：¥5-8（远低于上限）
- API调用次数：减少60-80%
- 任务完成率：保持100%
