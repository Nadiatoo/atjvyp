# 飞书 API 与 Token 优化方案

## 当前问题诊断

### 1. 飞书 API 调用问题
- **频繁发送**: 每条消息单独调用，容易触发频率限制
- **图片发送失败**: 媒体上传占用额外额度
- **无合并机制**: 短消息碎片化发送

### 2. Token 消耗问题
- **当前会话**: 125k input / 184 output
- **上下文累积**: 132k/256k (51%)，包含大量历史对话
- **重复计算**: 每次回复都携带完整上下文

---

## 优化方案

### 方案一：飞书 API 调用优化

#### 1. 消息合并策略
```python
# 当前做法（不好）
message.send("第一部分")
message.send("第二部分")  # 第二次调用
message.send("第三部分")  # 第三次调用

# 优化做法（好）
message.send("第一部分\n\n第二部分\n\n第三部分")  # 一次调用
```

#### 2. 批量发送替代实时发送
- **盘前推送**: 08:00 一次性发送（文字+图片合并）
- **盘后推送**: 15:35 一次性发送
- **盘中**: 不主动推送，用户询问再回复

#### 3. 图片优化
- **减少图片发送**: 优先用文字摘要，图片放云文档分享链接
- **压缩图片**: 降低分辨率减少上传时间
- **缓存机制**: 相同数据不重复生成图片

### 方案二：Token 消耗优化

#### 1. 上下文管理
```python
# 当前问题：每次对话都携带完整历史
# 优化：定期清理/归档旧对话

# 在 HEARTBEAT.md 中添加清理逻辑
if context_size > 100k:
    archive_old_messages()
    keep_only_recent(10)  # 只保留最近10条
```

#### 2. 精简输出
- **避免重复**: 不重复发送已发过的内容
- **摘要优先**: 详细分析放图片，文字只发结论
- **结构化**: 用表格/列表代替长段落

#### 3. 缓存常用回复
```python
# 缓存高频问题回复
cache = {
    "什么是四季判断": "春播夏长秋收冬藏...",
    "明天策略": "从缓存读取最新",
}
```

---

## 具体实施步骤

### 立即执行（今晚）

1. **修改消息发送逻辑**
   - 合并多条消息为一条
   - 图片改发云文档链接

2. **添加 Token 监控**
   - 每轮对话显示 Token 消耗
   - 超过阈值自动提醒

3. **上下文清理**
   - 会话超过 100k 时清理旧消息
   - 保留关键记忆到 MEMORY.md

### 明天执行（3月2日）

1. **测试新推送机制**
   - 08:00 一次性发送（文字+图片合并）
   - 监控 API 调用次数

2. **记录基准数据**
   - 记录优化前的 API 调用次数
   - 记录优化前的 Token 消耗

---

## 预期效果

| 指标 | 优化前 | 优化后 | 节省 |
|-----|--------|--------|------|
| 飞书 API 调用/天 | 5-10次 | 2次 | 60-80% |
| Token 消耗/会话 | 200k+ | <100k | 50% |
| 图片发送 | 3-5张 | 1张或0张 | 80% |

---

## 代码修改清单

### 1. 修改 `biaoge_backtest.py`
```python
# 添加消息合并函数
def send_combined_message(text, image_path=None):
    """合并发送文字和图片"""
    if image_path:
        # 上传图片到云文档，返回链接
        link = upload_to_feishu_drive(image_path)
        text += f"\n\n[查看图片]({link})"
    
    message.send(text)  # 只调用一次
```

### 2. 添加 Token 监控
```python
def log_token_usage():
    status = session_status()
    logger.info(f"Token: {status.tokens.in}/{status.tokens.out}")
    
    if status.context.percent > 80:
        logger.warning("上下文接近上限，建议清理")
```

### 3. 上下文清理脚本
```python
def cleanup_context():
    """定期清理旧对话"""
    # 只保留最近10条消息
    # 重要信息保存到 MEMORY.md
    pass
```

---

## 优化后的推送流程

### 盘前推送（08:00）- 一次调用
```
【彪哥战法】3月2日周一盘前策略

当前季节: 冬藏→春播过渡期
建议仓位: 20-30%

市场要点:
• 美伊冲突: 基准情形低开高走
• 两会窗口: 政策维稳预期
• 关注板块: 军工、黄金、油运、化工

[点击查看完整分析]
```

### 盘后推送（15:35）- 一次调用
```
【彪哥战法】3月2日周一盘后总结

今日表现:
• 上证指数: +0.5% (低开高走)
• 涨跌家数: 涨3000/跌2000
• 量能: 8000亿

季节判断: 春播信号确认 ✅
明日策略: 加仓至40-50%

[点击查看完整复盘]
```

---

## 监控指标

每天记录：
1. 飞书 API 调用次数
2. Token 输入/输出量
3. 会话上下文大小
4. 图片生成和发送次数

每周复盘：
- API 调用是否下降
- Token 消耗是否优化
- 用户体验是否受影响
